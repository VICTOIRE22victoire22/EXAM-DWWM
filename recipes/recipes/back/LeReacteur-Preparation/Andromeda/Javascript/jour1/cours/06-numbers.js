// Les nombres entiers positifs

const num1 = 10;

// Les nombres entiers négatifs

const num2 = -30;

// Les nombres décimaux, positifs ou négatifs

const num3 = 20.5; // ⚠️ on utilise l'écriture à point

let sum = 3 + 6;

// on peut utiliser le modulo (%) pour obtenir le reste d'une division :
const result = 10 % 3;
console.log(result); // 1

console.log(12348 % 2); // 0
